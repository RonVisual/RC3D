import { useState } from "react";
import {
  ArrowRight,
  ArrowUpRight,
  Box,
  ChevronRight,
  Menu,
  MessageCircle,
  Moon,
  Search,
  ShoppingBag,
  Sparkles,
  X,
} from "lucide-react";
import "./App.css";

const products = [
  {
    id: 1,
    name: "Arc Desk Stand",
    category: "Desk",
    price: "₹899",
    image:
      "https://images.unsplash.com/photo-1616627561950-9f746e330187?auto=format&fit=crop&w=900&q=80",
  },
  {
    id: 2,
    name: "Orbital Planter",
    category: "Decor",
    price: "₹699",
    image:
      "https://images.unsplash.com/photo-1485955900006-10f4d324d411?auto=format&fit=crop&w=900&q=80",
  },
  {
    id: 3,
    name: "Modular Tray",
    category: "Organization",
    price: "₹549",
    image:
      "https://images.unsplash.com/photo-1610701596007-11502861dcfa?auto=format&fit=crop&w=900&q=80",
  },
];

function App() {
  const [menuOpen, setMenuOpen] = useState(false);

  return (
    <div className="site">

      {/* NAVBAR */}
      <header className="navbar">
        <a className="logo" href="#">
          <span className="logo-mark">R</span>
          <span>RECREATIONALS</span>
        </a>

        <nav className="desktop-nav">
          <a href="#shop">Shop</a>
          <a href="#collections">Collections</a>
          <a href="#custom">Custom</a>
          <a href="#about">About</a>
        </nav>

        <div className="nav-actions">
          <button className="icon-button">
            <Search size={19} />
          </button>

          <button className="icon-button">
            <ShoppingBag size={19} />
            <span className="cart-count">0</span>
          </button>

          <button
            className="mobile-menu"
            onClick={() => setMenuOpen(!menuOpen)}
          >
            {menuOpen ? <X size={22} /> : <Menu size={22} />}
          </button>
        </div>
      </header>

      {/* MOBILE MENU */}
      {menuOpen && (
        <div className="mobile-nav">
          <a href="#shop" onClick={() => setMenuOpen(false)}>
            Shop
          </a>
          <a href="#collections" onClick={() => setMenuOpen(false)}>
            Collections
          </a>
          <a href="#custom" onClick={() => setMenuOpen(false)}>
            Custom
          </a>
          <a href="#about" onClick={() => setMenuOpen(false)}>
            About
          </a>
        </div>
      )}

      <main>

        {/* HERO */}
        <section className="hero">

          <div className="hero-content">

            <div className="eyebrow">
              <Sparkles size={14} />
              DESIGNED • PRINTED • YOURS
            </div>

            <h1>
              Objects made
              <br />
              <span>different.</span>
            </h1>

            <p className="hero-description">
              Thoughtfully designed 3D printed objects for your desk,
              your home, and everything in between.
            </p>

            <div className="hero-buttons">
              <a href="#shop" className="button button-primary">
                Explore collection
                <ArrowRight size={17} />
              </a>

              <a href="#custom" className="button button-secondary">
                Create something
              </a>
            </div>

            <div className="hero-meta">
              <div>
                <strong>01</strong>
                <span>Made to order</span>
              </div>

              <div>
                <strong>02</strong>
                <span>Custom designs</span>
              </div>

              <div>
                <strong>03</strong>
                <span>Small batches</span>
              </div>
            </div>

          </div>

          {/* HERO PRODUCT */}
          <div className="hero-product">

            <div className="hero-glow"></div>

            <div className="hero-product-image">
              <div className="fake-product">
                <div className="cube-top"></div>
                <div className="cube-front"></div>
                <div className="cube-side"></div>
              </div>
            </div>

            <div className="floating-card floating-card-top">
              <span className="floating-dot"></span>
              New collection
            </div>

            <div className="floating-card floating-card-bottom">
              <span>
                ARC / 01
                <small>Desk Series</small>
              </span>

              <ArrowUpRight size={17} />
            </div>

          </div>

        </section>

        {/* MARQUEE */}
        <section className="marquee">
          <div>
            3D PRINTED <span>✦</span>
            FUNCTIONAL <span>✦</span>
            CUSTOM <span>✦</span>
            DESIGNED LOCALLY <span>✦</span>
            3D PRINTED <span>✦</span>
            FUNCTIONAL <span>✦</span>
          </div>
        </section>

        {/* FEATURED */}
        <section className="section" id="shop">

          <div className="section-heading">

            <div>
              <p className="section-label">01 / SHOP</p>
              <h2>Made for your space.</h2>
            </div>

            <a href="#shop" className="view-all">
              View all
              <ChevronRight size={17} />
            </a>

          </div>

          <div className="product-grid">

            {products.map((product) => (
              <article className="product-card" key={product.id}>

                <div className="product-image">
                  <img src={product.image} alt={product.name} />

                  <button className="quick-add">
                    <ShoppingBag size={16} />
                    Add
                  </button>
                </div>

                <div className="product-info">

                  <div>
                    <p>{product.category}</p>
                    <h3>{product.name}</h3>
                  </div>

                  <strong>{product.price}</strong>

                </div>

              </article>
            ))}

          </div>

        </section>

        {/* COLLECTION */}
        <section className="collection-section" id="collections">

          <div className="collection-copy">

            <p className="section-label">02 / COLLECTIONS</p>

            <h2>
              Small objects.
              <br />
              <span>Big character.</span>
            </h2>

            <p>
              From practical desk accessories to pieces that simply
              make your shelf look better. Every object starts as an
              idea and ends as a physical thing.
            </p>

            <a href="#shop" className="text-link">
              Explore the collection
              <ArrowRight size={17} />
            </a>

          </div>

          <div className="collection-visual">

            <div className="visual-ring ring-one"></div>
            <div className="visual-ring ring-two"></div>

            <div className="visual-object">
              <Box size={100} strokeWidth={1} />
            </div>

            <div className="visual-label">
              <span>REC / 003</span>
              <small>Object study</small>
            </div>

          </div>

        </section>

        {/* CUSTOM */}
        <section className="custom-section" id="custom">

          <div className="custom-number">03</div>

          <div className="custom-content">

            <div className="eyebrow">
              <Sparkles size={14} />
              CUSTOM STUDIO
            </div>

            <h2>
              You imagine it.
              <br />
              <span>We print it.</span>
            </h2>

            <p>
              Have a design, idea, logo, character, or completely
              strange object you've always wanted? Send it our way.
            </p>

            <a href="#" className="button button-light">
              Start a custom order
              <ArrowUpRight size={17} />
            </a>

          </div>

        </section>

        {/* FEATURES */}
        <section className="features" id="about">

          <div className="feature">
            <Box size={23} />
            <h3>Made to order</h3>
            <p>
              Your product is printed when you order it. Less waste,
              more intention.
            </p>
          </div>

          <div className="feature">
            <Sparkles size={23} />
            <h3>Built your way</h3>
            <p>
              Choose materials, colors, sizes and custom details.
            </p>
          </div>

          <div className="feature">
            <MessageCircle size={23} />
            <h3>Real support</h3>
            <p>
              Questions? Talk to an actual person through our support
              channels.
            </p>
          </div>

        </section>

      </main>

      {/* FOOTER */}
      <footer>

        <div className="footer-top">

          <div className="footer-brand">
            <a className="logo" href="#">
              <span className="logo-mark">R</span>
              <span>RECREATIONALS</span>
            </a>

            <p>
              Objects, accessories and experiments
              made one layer at a time.
            </p>
          </div>

          <div className="footer-links">

            <div>
              <span>SHOP</span>
              <a href="#shop">All products</a>
              <a href="#collections">Collections</a>
              <a href="#custom">Custom</a>
            </div>

            <div>
              <span>HELP</span>
              <a href="#">Contact</a>
              <a href="#">Shipping</a>
              <a href="#">FAQ</a>
            </div>

            <div>
              <span>SOCIAL</span>
              <a href="#">Discord</a>
              <a href="#">Instagram</a>
              <a href="#">GitHub</a>
            </div>

          </div>

        </div>

        <div className="footer-bottom">
          <span>© 2026 Recreationals 3D</span>
          <span>Made with layers.</span>
        </div>

      </footer>

      {/* SUPPORT BUTTON */}
      <button className="support-button">
        <MessageCircle size={19} />
        <span>Need help?</span>
      </button>

    </div>
  );
}

export default App;
